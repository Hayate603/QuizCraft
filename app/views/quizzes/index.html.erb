<h1>クイズ一覧</h1>

<% if user_signed_in? %>
  <%= link_to '新しいクイズを作成', new_quiz_path %>
<% end %>

<ul>
  <% @quizzes.each do |quiz| %>
    <li>
      <%= link_to quiz.title, quiz %>
    </li>
  <% end %>
</ul>
